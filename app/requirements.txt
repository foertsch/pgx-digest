# Streamlit demo dependencies. Kept separate from pyproject.toml so the
# pgx_digest library itself stays UI-agnostic.

streamlit>=1.30
anthropic>=0.40
google-genai>=1.0
python-dotenv>=1.0

# pgx_digest's runtime deps — needed because `pgx_digest/__init__.py`
# eagerly imports the embeddings + learned-triage modules.
fastembed>=0.4
scikit-learn>=1.5
joblib>=1.3

# Install the local pgx_digest package itself.
-e ..
