.env
.env.local
*.env

# Local handoff doc — auto-loaded by Claude Code from disk;
# kept out of the public repo because it contains private
# project context, not generic project documentation.
CLAUDE.md

__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/

.DS_Store
.vscode/
.idea/

output/
results/
eval_results/
rag_cache/
*.log

# 1000 Genomes fixtures are regenerated on-demand (recipe in
# tests/fixtures/README.md). Keeping them out of the repo to avoid a
# ~90 MB blob — the repo commits precomputed statistics instead.
tests/fixtures/pharmcat_1kg_*.report.json
